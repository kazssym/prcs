# this is to keep Tk's paws off our command line arguments
set argv ""

#package require Tk 8
catch {load /usr/X11/lib/libX11.so}
# this is VERY Unix centric!
load [file dirname $tcl_library]/libtk8.0.so Tk

